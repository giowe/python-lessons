# Risolvere un sistema di 3 equazioni in 3 incognite applicando il metodo di Cramer

print('ax + by + cz = d')
a = float(input('inserisci "a" ' ))
b = float(input('inserisci "b" ' ))
c = float(input('inserisci "c" ' ))
d = float(input('inserisci "d" ' ))

print('a1x + b1y + c1z = d1')
a1 = float(input('inserisci "a1" ' ))
b1 = float(input('inserisci "b1" ' ))
c1 = float(input('inserisci "c1" ' ))
d1 = float(input('inserisci "d1" ' ))

print('a2x + b2y + c2z = d2')
a2 = float(input('inserisci "a2" ' ))
b2 = float(input('inserisci "b2" ' ))
c2 = float(input('inserisci "c2" ' ))
d2 = float(input('inserisci "d2" ' ))

#calcolare il determinante della matrice D

#se det = 0 il sistema e'indeterminato ed esce

#calcolo il determinante della matrice delle X   (detX)

#calcolo il determinante della matrice delle Y   (detY)

#calcolo il determinante della matrice delle Z   (detZ)

#calcolo il valore della X Y Z

#stampo i risultati

